{
  "name": "@elements/job",
  "description": "A central job tracking class.",
  "version": "0.0.0",
  "license": "MIT",
  "browser": {
    "./find": false
  },
  "engines": {},
  "dependencies": {
    "@elements/term": "0.0.0",
    "@elements/utils": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "3.9.7"
  },
  "scripts": {
    "clean": "rm -rf .elements/dist",
    "copyfiles": "mkdir -p .elements/dist && cp README.md LICENSE.txt package.json .elements/dist",
    "build": "yarn run clean && yarn run copyfiles && tsc",
    "build:watch": "yarn run clean && yarn run copyfiles && tsc --watch"
  }
}
